# yggdrasil

Tree generation and traversal package for hierarchical solvers.

`yggdrasil` provides Morton encoding, radix tree construction, geometry helpers,
dual-tree traversal, and interaction buffer representations. It is designed to
be consumed by solver packages such as `jaccpot`.
